这个main.c是干什么的，整车运行的时候main.c是在什么时候调用的?
cubeMX中的main.c在运行过程中的调用顺序

```
整车上电 / MCU 复位
  ↓
startup_stm32f407xx.s 里的 Reset_Handler
  ↓
初始化栈、拷贝 .data、清零 .bss
  ↓
调用 SystemInit()
  ↓
调用 main()
  ↓
main.c 里初始化 HAL、时钟、GPIO、串口、BSP、FreeRTOS
  ↓
osKernelStart()
  ↓
进入 FreeRTOS 任务调度，后面主要跑各个任务
```


## CAN1的配置

TEC (Transmit Error Counter)：发送错误计数器。
REC (Receive Error Counter)：接收错误计数器。