# AGENTS.md

## 工作规则

- 每完成一个独立改动步骤后，立即检查 `git status --short`。
- 只提交本步骤实际修改的文件，不把用户已有的无关改动加入提交。
- 提交前尽量运行与本步骤相关的最小验证命令；如果无法运行，需在回复中说明原因。
- 每次提交使用清晰、具体的提交信息，格式建议为：
  - `feat: ...`
  - `fix: ...`
  - `docs: ...`
  - `chore: ...`
- 如果工作区已有未提交改动，先识别哪些是本次新增，避免覆盖或回滚用户改动。
- 除非用户明确要求，不执行 `git reset --hard`、`git checkout -- <file>` 等会丢弃改动的命令。

## 自动提交要求

当代理对仓库做出任何文件改动时，应在该改动步骤完成后自动执行一次提交：

1. 查看当前状态：`git status --short`
2. 暂存本步骤修改的文件：`git add <files>`
3. 提交：`git commit -m "<type>: <summary>"`
4. 在最终回复中列出提交哈希和提交信息

如果提交失败，应继续诊断原因；如果因权限、钩子、测试失败或用户已有改动冲突而无法提交，应明确说明阻塞点。
